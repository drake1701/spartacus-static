<div class="col-xs-12">
    <p class="entry-title">
        <a href="{{url}}">{{title}}</a>
    </p>
    <p class="entry-date">{{publishedAt_short}}</p>
    <div class="entry-image">
        <a href="{{url}}" title="{{title}}">
            <img class="lazy" data-original="{{thumb}}" alt="{{title}}">
        </a>
    </div>
</div>